name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-all:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Build all
        run: make build-all
